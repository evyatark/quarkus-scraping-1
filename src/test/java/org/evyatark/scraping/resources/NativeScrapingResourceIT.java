package org.evyatark.scraping.resources;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeScrapingResourceIT extends ScrapingResourceTest {

    // Execute the same tests but in native mode.
}